---
"@couch-kit/cli": minor
---

Add `replay` command for replaying recorded game sessions against a reducer. Add `dev` command for starting a Vite dev server with LAN URL output. Improve `init` scaffolding with package manager detection, stricter TypeScript config, sample game reducer, and .gitignore generation.
