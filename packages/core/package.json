{
  "name": "@couch-kit/core",
  "version": "0.9.0",
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "description": "Shared types, protocol, and game reducer for Couch Kit party games",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/faluciano/react-native-couch-kit.git",
    "directory": "packages/core"
  },
  "homepage": "https://github.com/faluciano/react-native-couch-kit#readme",
  "keywords": [
    "react-native",
    "couch-kit",
    "party-game",
    "game-state",
    "reducer"
  ],
  "main": "./dist/index.js",
  "types": "./lib/index.d.ts",
  "exports": {
    ".": {
      "types": "./lib/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "src",
    "dist",
    "lib",
    "README.md",
    "CHANGELOG.md"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target browser && bun build ./src/index.ts --outfile ./dist/index.cjs --target browser --format cjs && tsc -p tsconfig.build.json",
    "prepublishOnly": "bun run build",
    "test": "bun test",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist lib"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
